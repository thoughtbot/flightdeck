apiVersion: v1
kind: ConfigMap

metadata:
  annotations:
    meta.helm.sh/release-name: aws-auth
    meta.helm.sh/release-namespace: kube-system
  labels:
    app.kubernetes.io/managed-by: Helm
  name: aws-auth
  namespace: kube-system

data:
  mapRoles: |
    {{ toYaml .Values.mapRoles | indent 4 | trim }}
